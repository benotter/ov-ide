import { OttProgActTypes, OttProgInitial } from './ott-prog-actions';

export function OttProgReducer(state = OttProgInitial, act)
{
    switch(act.type)
    {
        default:
            return state;
    }
}